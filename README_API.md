# ☕ API Documentation – Coffee Lover Website

Документація описує **routes (сторінки)** та **API-ендпоінти**, які логічно відповідають HTML-сайту **Coffee Lover**.
Сайт є статичним (HTML/CSS/JS), але використовує JavaScript для взаємодії з даними (каталог товарів, кошик, замовлення).

---

## 🔗 Base URL
```
https://coffeelover.example
```

API (умовний / навчальний):
```
https://api.coffeelover.example
```

---

## 🌐 Routes (сторінки сайту)

| Route | Метод | Опис |
|------|------|------|
| `/index.html` | GET | Головна сторінка магазину кави |
| `/login_frame.html` | GET | Вхід / реєстрація користувача |
| `/shop` | GET | Каталог кави (умовно) |
| `/contacts` | GET | Контактна інформація |
| `/privacy-policy` | GET | Політика конфіденційності |

---

## ☕ API Endpoints

### 1️⃣ Отримати список кави
**Endpoint:**
```
GET /products
```

**Опис:** Повертає список кавових продуктів для відображення на головній сторінці

**Response (JSON):**
```json
[
  {
    "id": 1,
    "name": "Blooming Spring",
    "price": 379,
    "description": "100% арабіка Colombia El Llano",
    "image": "blooming-spring.jpg"
  }
]
```

---

### 2️⃣ Отримати інформацію про конкретний товар
**Endpoint:**
```
GET /products/{id}
```

**Опис:** Повертає детальну інформацію про вибрану каву

---

### 3️⃣ Додати товар до кошика
**Endpoint:**
```
POST /cart
```

**Request:**
```json
{
  "productId": 1,
  "quantity": 1
}
```

---

### 4️⃣ Отримати вміст кошика
**Endpoint:**
```
GET /cart
```

---

### 5️⃣ Створити замовлення
**Endpoint:**
```
POST /order
```

**Request:**
```json
{
  "customerName": "Denis",
  "phone": "0676364080",
  "items": [
    { "productId": 1, "quantity": 2 }
  ],
  "total": 758
}
```

**Response:**
```json
{
  "status": "success",
  "orderId": 202
}
```

---

### 6️⃣ Зворотний звʼязок
**Endpoint:**
```
POST /feedback
```

**Request:**
```json
{
  "name": "Олена",
  "email": "user@mail.com",
  "message": "Дуже смачна кава!"
}
```

---

## ⚙️ Використання API (JavaScript)
```javascript
fetch('https://api.coffeelover.example/products')
  .then(response => response.json())
  .then(data => console.log(data));
```

---

## 🌍 Зовнішні API, що використовуються в JavaScript

### 🎨 Art Institute of Chicago API
Використовується для завдання з API (завантаження зображень мистецтва).

**Endpoint:**
```
GET https://api.artic.edu/api/v1/artworks
```

**Query parameters:**
| Параметр | Значення | Опис |
|--------|----------|------|
| `limit` | `3` | Кількість обʼєктів |

**Використання у коді:**
```javascript
fetch("https://api.artic.edu/api/v1/artworks?limit=3")
```

**Поля відповіді, які використовуються:**
- `data[].image_id` — ідентифікатор зображення
- `data[].title` — назва роботи

**Формування URL картинки:**
```
https://www.artic.edu/iiif/2/{image_id}/full/200,/0/default.jpg
```

---

## ⚙️ JavaScript-функціонал (без серверного API)

| Функція | Опис |
|-------|------|
| `changeBg` | Зміна фону сторінки |
| `showImages()` | Динамічне додавання зображень |
| `fetchArtImages()` | Отримання даних з API |
| `comparePhrases()` | Пошук спільних слів |
| `loginForm submit` | Валідація форми логіну |

---

## 📌 Примітки
- Сайт є **клієнтським (frontend-only)**
- API використовується через **fetch + async/await**
- Дані з API виводяться динамічно в DOM
- Помилки обробляються через `try/catch`

---

## 📎 Посилання на документацію API
- Art Institute of Chicago API: https://api.artic.edu/docs/

---

**Проєкт:** Coffee Lover ☕  
**Автор:** Deniska-meow

